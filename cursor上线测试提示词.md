# 上线前全量测试 · 给 Cursor 的提示词

> 使用方法：不要一次性全部粘贴。按【第 0 轮】→【第 1 轮】→【第 2 轮】的顺序分轮发给 Cursor，每轮做完确认没问题再发下一轮。每一轮开头的"通用规则"都要带上（可以放在 Cursor 的 Rules 里只写一次）。

---

## 通用规则（建议写进 Cursor Rules，全程生效）

```
你正在维护一个即将交付上线的系统，遵守以下规则：
1. 严格遵守仓库根目录 AGENTS.md 和 apps/web/AGENTS.md 的所有约定（Hasura BYO JWT、只有 CLI 迁移能改 schema、密钥不进前端等）。
2. 每修复一个问题，用一句话说明根因和修法，不要静默改代码。
3. 每完成一批修改，必须运行 npm run lint 和 npm run build（在 apps/web 目录下），确保零错误；有警告要列出来。
4. 不破坏现有 GraphQL 查询和 API 路由的对外契约；如需改动先给方案等我确认。
5. 发现的问题按【阻断上线 / 严重 / 一般 / 建议】四级分类，记录到 apps/web/上线测试报告.md，包含：问题描述、复现路径、文件位置、严重级别、修复状态。
6. 遇到以下情况必须停下来明确向我索要，禁止编造或跳过：
   - 需要真实账号密码、JWT、API Key、后端地址
   - 需要真实照片或文件来测试上传/AI 识别
   - 需要真实手机或微信环境验证
   - 需要我人工判断 UI 效果或业务逻辑是否正确
   索要的格式统一为："【需要你提供】xxx，用途：xxx"
7. 每次只改当前轮次范围内的东西，不要顺手重构无关代码。
```

---

## 第 0 轮：跑起来 + 静态排雷

```
背景：这是一个即将交付上线的巡检管理系统。技术栈：Next.js 16 + React 19 + TypeScript，
单体仓库，唯一应用在 apps/web；PC 管理端用 antd + echarts，移动端用 react-vant；
后端是 Hasura GraphQL（BYO JWT 认证），还有图片上传和 AI 视觉识别接口。

本轮目标：把项目完整跑起来并做静态排雷，不做功能测试、不改 UI。

1. 在 apps/web 下执行 npm run lint 和 npm run build，修复所有报错；警告逐条列出并评估是否需要修。
2. 全仓扫描并修复以下隐患：
   - 硬编码的地址、密钥、测试数据、TODO/FIXME、遗留 console.log / debugger
   - 未捕获的 Promise、缺少错误处理的 fetch/GraphQL 调用
   - 列表页缺少 loading / 空数据 / 加载失败三种状态的地方
   - 表单缺少校验或可以重复点击重复提交的地方
   - localStorage / JWT 使用是否有安全隐患（XSS、过期未处理）
   - Next.js Image、动态导入、SSR/客户端组件边界是否有误用
3. 检查 apps/web/Dockerfile 和根目录 docker-compose.yml 能否对应上，构建参数和环境变量是否齐全。
4. 把发现的问题按四级分类写入 apps/web/上线测试报告.md。

如果发现缺少环境变量或配置导致跑不起来，列出"【需要你提供】"清单后停下等我。
```

---

## 第 1 轮：逐模块功能回归测试

```
本轮目标：对系统全部功能做回归测试，修复功能性 bug。UI 美化留到下一轮，本轮只修"坏了的"。

请按以下模块逐个过一遍（代码位置供参考：PC 端在 src/views 和 src/app/(main) 下，
移动端在 src/m、src/app/m 下，接口在 src/app/api 下）：

【认证与入口】
- 登录页：错误密码、空表单、token 过期后的跳转
- choose-role 角色选择：各角色进入后权限是否正确、越权访问其他角色页面会怎样
- 403 页面是否生效

【移动端（员工使用，重点中的重点）】
- 完整走一遍业务流程：登录 → 任务列表 → 开始巡检(start) → 拍照(photo) → 
  AI 识别(vision/analyze) → 填写报告(report) → 提交 → 成功页(success)
- 拍照上传：图片过大、格式不对、上传中断、重复上传
- 站点(sites)：定位/地图(geocode + leaflet)是否正常
- 收入(income)、财务案件(finance-cases)、我的(my)、设置(settings)各页面
- 断网/弱网下的表现、返回上一页状态是否保留

【PC 管理端】
- dashboard 和分析(analysis)页：echarts 图表在无数据、数据量极大时的表现
- 审核(audit)：通过/驳回流程、必填意见、并发审核
- 案件(cases)、记录(records)、任务(tasks)、模板(templates)：增删改查、分页、筛选、导出 Excel（exceljs）
- 财务(finance)、收入(income)：金额计算精度（浮点数问题）、负数、大数
- 硬性规则(hard-rules)、禁用项(forbidden)：规则生效逻辑
- 站点(sites)：地图选点、省市区选择(element-china-area-data)
- 用户(users)、设备(devices)、设置(settings)：权限边界，普通管理员能否改超管

【接口层】
- /api/health、/api/auth、/api/upload/image、/api/upload/token、/api/vision/analyze、
  /api/geocode、/api/bff、/api/config、/api/system：异常入参、未带 JWT、超时

每个模块测完在 上线测试报告.md 里更新状态。
凡是需要真实账号、真实照片、真实后端才能验证的，汇总成"【需要你提供】"清单给我，先测能测的。
```

---

## 第 2 轮：UI 优化 + 上线前收尾

```
本轮目标：在不改变功能的前提下优化 UI，然后做上线前检查。

【UI 优化原则】
1. 先改一致性，再改美观：统一主色、字号层级、间距（8 的倍数）、圆角、按钮风格。
2. 所有列表/表单页补齐 loading 骨架屏、空状态插画或提示、错误重试按钮。
3. 移动端（react-vant）：检查安全区适配、按钮点击区域 ≥44px、
   长列表滚动性能、弹层遮罩、字体在 iOS/安卓的表现。
4. PC 端（antd）：表格列宽溢出、长文本省略、弹窗表单重置、面包屑/页头统一。
5. 图表页：配色统一、tooltip 数字格式化（千分位、单位）、空数据显示"暂无数据"。
6. 每改一个页面，截图或描述改动点，涉及交互变化的先给我方案再动手。
7. 禁止为了美观改变业务逻辑或删除功能。

【上线前收尾】
1. 全仓检查：无 console.log、无 mock 数据、无测试账号残留、无注释掉的死代码块。
2. 环境变量梳理：列出所有必需环境变量及用途，写入 apps/web/.env.example（只写变量名不写值）。
3. npm run build 零错误零警告通过；Dockerfile 能独立构建。
4. 确认 /api/health 可用于部署后健康检查。
5. 更新 上线测试报告.md：所有问题标记最终状态，遗留问题写明风险和上线后跟进建议。
6. 最后输出一份《上线检查清单》：部署步骤、需要我在服务器上配置的东西、回滚方案。
```

---

# 需要你（老板本人）配合的清单

Cursor 在测试过程中大概率会来找你要这些东西，建议提前准备好：

| 序号 | 需要你提供/做的事 | 用在哪 |
|---|---|---|
| 1 | **各角色测试账号**（管理员、审核员、巡检员工） | 登录、权限、审核流程测试 |
| 2 | **真实巡检照片 5~10 张**（光线好/差、角度正/歪各来点） | 移动端拍照上传 + AI 视觉识别测试，这是最容易出问题的环节 |
| 3 | **后端的可用地址和密钥**（HASURA_JWT_SECRET、GraphQL 地址、AI 接口 key） | 写进 apps/web/.env.local，没这些 Cursor 只能干跑前端 |
| 4 | **一部真实手机**（最好安卓+iOS 各一，微信内置浏览器也开一下） | 移动端最终验收，模拟器测不出所有问题 |
| 5 | **业务判断** | 审核流、规则配置、金额计算这些地方，AI 不知道"对不对"，只能你拍板 |
| 6 | **UI 审美确认** | 第 2 轮改界面时，让它每改一个模块就停下给你看效果，别让它一口气全改完 |
| 7 | **服务器/部署环境信息** | 最后出《上线检查清单》时需要：部署到哪、域名、HTTPS、反向代理方式 |

## 几个实战建议

1. **分轮发，别一口气全贴**——一次让它测全部，它会顾此失彼、越改越乱。
2. **每轮结束先 git commit**，出问题随时能回滚到上一轮的状态。
3. **它报"修好了"的地方，涉及钱、权限、提交动作的，你自己手动点一遍再信**。
4. **报告文件让它持续维护**（apps/web/上线测试报告.md），最后这份报告本身就是交付材料的一部分。
