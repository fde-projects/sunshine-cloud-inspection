services:
  web:
    image: registry.truss3.weweknow.com/sunshine-cloud-inspection/web:${IMAGE_TAG:-latest}
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
